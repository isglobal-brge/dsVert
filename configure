#!/bin/sh
# Installation is intentionally side-effect free. The process serving dsVert
# creates its owner-only persistent state atomically on the first cryptographic
# service operation, under that process's identity and never during an image or
# package build.
exit 0
