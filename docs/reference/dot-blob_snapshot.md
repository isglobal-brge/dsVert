# Read ALL blobs into a named list (memory + disk merged)

Read ALL blobs into a named list (memory + disk merged)

## Usage

``` r
.blob_snapshot(ss)
```

## Arguments

- ss:

  Session environment

## Value

Named list of blob values
