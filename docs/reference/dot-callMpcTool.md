# Call dsvert-mpc with JSON input

Call dsvert-mpc with JSON input

## Usage

``` r
.callMpcTool(command, input_data)
```

## Arguments

- command:

  The dsvert-mpc subcommand to run

- input_data:

  List that will be converted to JSON input

## Value

Parsed JSON output from dsvert-mpc
