# Get or create the session's temp directory (lazy)

Get or create the session's temp directory (lazy)

## Usage

``` r
.ensure_session_dir(ss)
```

## Arguments

- ss:

  Session environment

## Value

Character path
