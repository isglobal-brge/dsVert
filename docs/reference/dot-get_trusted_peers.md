# Get the set of trusted peer identity PKs

Get the set of trusted peer identity PKs

## Usage

``` r
.get_trusted_peers()
```

## Value

Character vector of base64 PKs, or NULL if enforcement disabled.
