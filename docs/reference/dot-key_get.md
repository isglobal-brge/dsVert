# Read a persistent key (memory first, then disk)

Read a persistent key (memory first, then disk)

## Usage

``` r
.key_get(name, ss)
```

## Arguments

- name:

  Character. Key name

- ss:

  Session environment

## Value

Character string (or vector for multi-line), or NULL
