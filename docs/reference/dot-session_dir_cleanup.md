# Clean up all session storage (memory + disk with secure deletion)

Clean up all session storage (memory + disk with secure deletion)

## Usage

``` r
.session_dir_cleanup(ss)
```

## Arguments

- ss:

  Session environment
