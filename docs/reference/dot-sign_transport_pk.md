# Sign a transport PK with the identity SK

Sign a transport PK with the identity SK

## Usage

``` r
.sign_transport_pk(transport_pk_b64, identity_sk_b64)
```

## Value

Character. Standard base64 signature.
