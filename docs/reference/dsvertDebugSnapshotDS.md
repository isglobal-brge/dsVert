# Snapshot a session slot to another key (task \#116 G diagnostic)

Snapshot a session slot to another key (task \#116 G diagnostic)

## Usage

``` r
dsvertDebugSnapshotDS(slot_key = NULL, snapshot_key = NULL, session_id = NULL)
```

## Arguments

- slot_key, snapshot_key, session_id:

  Character.

## Value

list(copied = TRUE).
