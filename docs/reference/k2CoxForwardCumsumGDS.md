# Compute the forward cumsum `G_j = sum over i<=j with delta=1 of recip(i)`

Compute the forward cumsum
`G_j = sum over i<=j with delta=1 of recip(i)`

## Usage

``` r
k2CoxForwardCumsumGDS(session_id = NULL)
```

## Arguments

- session_id:

  GLM session id.

## Value

list(G_length)
