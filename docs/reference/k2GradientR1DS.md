# Gradient round 1: compute (X-A, r-B) in selected ring (Ring63 / Ring127)

Gradient round 1: compute (X-A, r-B) in selected ring (Ring63 / Ring127)

## Usage

``` r
k2GradientR1DS(peer_pk, session_id = NULL)
```

## Arguments

- peer_pk:

  Character (base64url). Peer party's transport public key for sealed
  shares.

- session_id:

  Character. Active MPC session identifier.
