# Receive peer's shared data (FixedPoint)

Receive peer's shared data (FixedPoint)

## Usage

``` r
k2ReceiveShareDS(peer_p = NULL, session_id = NULL)
```

## Arguments

- peer_p:

  Integer. Number of features held by the peer (sets share length).

- session_id:

  Character. Active MPC session identifier.
