# Force garbage collection on the server

Triggers R garbage collection without removing session state. Used
periodically during long-running protocol loops.

## Usage

``` r
mpcGcDS()
```

## Value

TRUE
