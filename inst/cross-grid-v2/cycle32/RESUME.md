# Cycle32 — four simple promotions; NB stale-defect resolution

**4/10 promoted:** NB, LASSO, multinomial, ordinal, exclusively at fleet source
**f3795a2afc4fbb03eb7137ee3fbccefc75748c76 / e748f0590168a671160f1c96dd49b3b9fa395ab2**.
Current execution source remains **9f9d435/b9d2f6a**; this cycle changes evidence
and documentation only. Do not transfer fleet measurements to current runtimes.

Each family has nine actual n2000/p6/J2 cells (epsilon 1/4/8; K2/K3/K5),
independent Go oracle equality, sticky replay, durable-batch interruption/recovery,
direct R cold replay and tamper rejection. Fresh audit verifies archives,
original/instrumented harness hashes, source pins and measured release capacity.
All 36 pass. Exact-fleet server paired: 1164 tests/21062 assertions, no nonpasses.
David's cycle32 G accepts generic v1.2.0/v1.2.1 exported-client validation and
makes per-family client paired confirmatory; this is the authorization basis,
not a new execution of the historical generic battery. Client suite remains
active at 12:13 UTC; all 2504 frozen source hashes unchanged. Any later
family-specific client defect must be recorded and the promotion flagged.

## NB math gate resolved, without a new patch or narrowed domain

The historical zero-loss warning was superseded by the next STATUS entry and
commit **5a7f1471d1445915e5ae68dff133dac0d1ebe832**, an ancestor of fleet source.
The old signed expression compiled the positive multiplier incorrectly as
8*y-32 at theta=4. The committed fix constructs 8*y+8*theta in uint128 before
conversion. It predates every retained fleet NB release; all nine post-fix
cells already re-passed. Cycle32 re-audits those actual releases, rather than
claiming nine newly executed releases or launching prohibited duplicates.

Fresh isolated archive of exact fleet source: five native regressions PASS
in 32.548 seconds. Includes the two original cases, independent likelihood
checks, all eleven theta profiles, K2/K3/K5 batches, certified width/domain
boundaries and invalid-input rejection. All 978 source files unchanged.
NB implementation and the selected regressions match current source bytewise.

A separate isolated control changes only the multiplier expression back to
its old form: the original regression FAILS, producing valid zero losses for
both cases. Fixed outputs are **117694** and **173378**, exactly the independent
integer oracle. No privacy, caps, rounding, arithmetic domain or runtime edits.
See NB_PROOF.json, NB_SOURCE.json, nb-regression.log and nb-old-expression.log.
The failed first archive attempt (nonexistent optional path) is recorded.

## Promotion and capacity artifacts

`integrator-evidence/PROMOTION_MANIFEST.jsonl` contains four completed promotion
records, not launch requests. `PROMOTIONS.json` and `SIMPLE_RECONCILIATION.json`
retain every cell and source/evidence hash. Maxima over already measured cells:

| Family | Serialized aggregate RPC bytes | Release seconds |
|---|---:|---:|
| NB | 3,563,865,032 | 4,513.600 |
| LASSO | 2,847,066,650 | 3,862.188 |
| Multinomial | 6,879,476,337 | 7,021.332 |
| Ordinal | 4,196,242,255 | 4,946.680 |

All below 256GB/6h on the recorded serialized aggregate-RPC basis. These are
measured release scopes, not native wire bytes or a newly measured envelope.
The 24-row heavy RELEASE_MANIFEST remains unchanged at 9f9d435/b9d2f6a:
four epsilon8 jobs/family, 12 LMM/GLMM ready; separate 1080 oracle-only rows.
No heavy job duplication or claim that unready Cox/GEE jobs can run.

## Shared Gaussian diagnosis and ongoing work

Cycle28's real signed base/recovery diagnosis and cycle31's current native
regression remain applicable. Both encounter a LIVE optional Gaussian support
gap and choose the SAME certified Laplace fallback. Required Gaussian support
16693975 exceeds 1048576; its certified radius loses to Laplace. No wrong scale
or replay-only divergence was found. Pod16's terminal failure is alignment-mask
worker readiness, with native cause still unresolved. No shared sampler patch
or new mathematical blocker is justified by that handled Gaussian diagnostic.
Shared DP remains owned here; GEE must not fork a fix. No GEE-owned edit.

Fresh frozen LMM observation at 12:14 UTC still has no completed n2000 K2
result; no restart or mutation. Fresh reviewer fleet observation is retained.
Remaining work: LMM relay batching with fresh output/recovery equivalence;
Cox signed source orchestration and authenticated DP-vector reader (N<=400);
heavy topology/recovery/cold/tamper/paired/capacity gates; harvest confirmatory
simple client suite. No new real release, tag, push or thesis edit this cycle.
