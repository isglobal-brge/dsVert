# Cycle35 — Cox shared DP lifecycle hooks

The shared Synopsis exact-GC compiler now binds the Cox public terminal plan;
the post-START source release-range path injects durable Ring128 candidate
shares and private validity through the existing Cox adapter. Automatic Cox
context lookup retrieves schema bytes from the MAC-authenticated Synopsis
cache and verifies signatures/attestation, signed contract and source layout.
Schema retention is extended to Cox manifests. Non-exact Synopsis sampling
and legacy vector PREPARE/START reject Cox before private reads/claims, because
those paths cannot preserve staged validity. Other families retain their path.

Public Cox workload admission/source registration/remote orchestration and
client compile/portable-certificate dispatch remain closed. This is not an
actual Cox DP release, recovery/capacity result or promotion. N<=400 staged
scope and integer arithmetic remain unchanged. The existing internal client
reader remains at 5b6458f.

Priority F: native planner/sampler unchanged. The cycle28 diagnosis remains:
optional Gaussian support gap is live in base/recovery, both choose the same
certified Laplace fallback. No wrong sigma or recovery-only mismatch found.
Pod16's terminal alignment-mask native readiness cause remains unresolved.
Shared DP belongs to this lane; GEE must not fork a sampler patch for the caught
Gaussian diagnostic. No support/privacy change or math blocker.

4/10 promoted: NB, LASSO, multinomial, ordinal, exclusively at f3795a2/e748f05.
NB's resolved math gate remains closed. Confirmatory client paired remains
incomplete at 12:49 UTC; all 2504 frozen source hashes unchanged. Server paired
remains 1164 tests/21062 assertions PASS. Frozen cycle16 LMM K2 incomplete at
12:49 UTC; all 2553 source hashes unchanged, obsolete stopped queue untouched.
Reviewer pods12/14/15 observed only, retaining original 502b005/4c6c562 jobs.

Next: join Cox signed source registration/remote binding/routing and public
compile/certificate dispatch; prove a fresh small authenticated full DP release
before marking Cox fleet-ready. LMM compile reuse already exists; relay batching
still needs fresh baseline/output/bilateral+unilateral recovery equality and
separate capacity measurement. Complete heavy promotion gates; harvest simple
client paired; retain worker startup logs on reviewer-owned pod16 retry.
No GEE-owned edits, frozen mutation, new real release, tag, push or thesis edit.

Test provenance: r2 covers Cox/native, cached schema, validity guards and shared
LMM/GLMM/Synopsis safety. Its final new compiler test (also in r1) correctly
rejected a test-double contract that omitted the returned stage digest. r3
changes only that test, preserves the rejection as a negative assertion and
reruns the exact-GC file plus real-native Gaussian preflight. Production code
and runtimes are byte-identical between r2/r3; all attempts are retained.

LMM_BATCHING_NEXT.md records current compile reuse and the relay ACK boundary.
The configurable 8 MiB transport ceiling is not a portable DSLite scalar limit;
keep bounded individual payloads in any future authenticated batch design.

Original oracle-only selection campaign: 63/540 complete at
2026-09-20T13:05:29.790217+00:00, exclusively fcbe557/4c6c562;
all frozen source hashes unchanged. Partial reliability, not real release or capacity.

Execution pair: 8214373e62b7923d99ff8366b6f7b705285c3626/5b6458fa7264909eaa1bbdb63d8b0ccc22acc40a.
Final source-verified proof: 54 tests/2154 assertions, zero nonpasses;
all 2674 final source hashes unchanged and equal committed source.
Minimal manifests: 24 real / 1080 oracle-only definitions; all nine exact
commitments independently regenerated and equal prior integers.
