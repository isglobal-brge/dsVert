# Cycle45 checkpoint — 2026-09-20

Execution source **e25bb19/bf73744**. Server executable
source is unchanged; the cycle45 server checkpoint is evidence only. Consult
MANIFEST_VALIDATION.json for the final committed manifest pair.

**37 tests / 1,079 assertions PASS**, zero nonpasses, in a fresh immutable local
snapshot. Client changes connect Cox discovery, padded source layout, signed
schema/contract preflight, public arithmetic reconstruction, and generic source
dispatch into the existing owner-first authenticated durable executor. The
Synopsis runner passes signed schema JSON through the dispatcher; that schema
is validated before any Cox source RPC. K2/K3/K5 full layout commitments match
the server, all source owners remain represented, and time values remain private.
Reconstruction checks numeric/sensitivity/source-contract bindings and the
requested dataset/analysis/owner/adjacency/capacity/scale. N<=400 enforced.

Tests use real Ed25519 authentication. Transport and native invocation in
orchestration are test doubles; persisted-state combinations test client
branching, not an actual native crash/recovery. Existing cold/tamper, LMM,
categorical and GLM regressions pass. An initial GLM harness error from loading
two pkgload packages was resolved with a fresh client-only R process. No
production change followed the frozen passing proof. SOURCE_HASHES.json binds
all 19373 snapshot files; changed files equal committed source.

**Cox is still not fleet-ready.** Contract runtime states and the server workload
admission gate remain closed. Discovery alone does not admit a contract-only
artifact; the focused test enforces that distinction. No real Cox DP release,
capacity result or promotion is claimed.

Next coordinated wiring before the FIRST signed small release:
1. Server dpCapsuleWorkload.R artifact loop: replace explicit Cox rejection with
   full signed schema/contract validation + Cox workload projection, including
   the same sensitivity accumulation. Coordinate contract runtime states in
   crossgrid_cox.R and client dp_cox_grid_cross.R. Server generic discovery in
   dpGLMGridCrossMaterializer.R still omits Cox; connect it to the existing Cox
   source/transport/Claim paths, not the outcome-based GLM projection.
2. Client dp_synopsis_vector_runner.R: collect Cox publication evidence for new
   and cold runs, including compilation-only cold replay (no private Claim reread).
   dp_gaussian_certificate.R needs Cox descriptor acceptance, mandatory signed
   Cox evidence, authenticated reconstruction, integer-lattice bounds and Cox
   moment dispatch. Reuse .dsvert_dp_cox_cross_public_evidence_set and the
   existing authenticated Cox cold vector reader. Public release function in
   dp_cox_grid_cross.R still deliberately rejects and must be wired here.
3. validate_structured_dslite.R: Cox is still outside staged harness branches;
   add pinned time/event-owner garbler, actual Cox recovery/cold/tamper hooks
   and appropriate <=400 fixture shape. No GEE-owned sections may be changed.
4. Run actual signed small source -> native -> joint-DP lifecycle with independent
   integer oracle and both native recovery paths; only after success emit four
   ready Cox fleet definitions and launch the first wave.

**4/10 promoted**, at f3795a2/e748f05; NB resolved. Uniform release envelope
**256 GB / 8 h**. Fresh fleet observation remains **10 running / 2 terminal**;
retained original-pair records remain **1 PASS / 1 FAIL**. No new result to
promote, no wave rerun, no source reattribution. Measured capacity is unchanged
from cycle43. Deferred-batching paired suite remains incomplete; all3025 frozen
files match. Its separate n4 smoke remains PASS, attributed only to its own pair.

Manifest must retain 12 ready LMM/GLMM definitions (four/family), Cox/GEE
placeholders, separate oracle-only grids and the explicit instruction to harvest/
re-score the current wave rather than rerun it. Shared optional-Gaussian handling
remains integration-owned and unchanged; GEE must not duplicate that patch.
No native/runtime, GEE-owned, batching, simple release, tag, push or thesis change.
No new math blocker.
