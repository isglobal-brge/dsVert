% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mpcUtils.R
\name{.k2_enforce_K}
\alias{.k2_enforce_K}
\title{Enforce K-arity at server side (defense-in-depth)}
\usage{
.k2_enforce_K(ss, expected_K, fn_name = NULL)
}
\arguments{
\item{ss}{MPC session state (output of \code{.S(session_id)}).}

\item{expected_K}{Integer expected K (e.g. 2L for K=2-only paths).}

\item{fn_name}{Optional function name embedded in the error message
to aid log-trace attribution.}
}
\value{
\code{TRUE} invisibly on success; \code{stop(...)} on K mismatch.
}
\description{
Refuses calls into a K-specific MPC primitive when the calling
deployment's K does not match \code{expected_K}. The K value is derived
from \code{ss$peer_transport_pks}, populated by \code{mpcStoreTransportKeysDS}
AFTER Ed25519 signature verification + trusted-peers list check
performed by \code{.verify_all_peer_identities} (mpcUtils.R:530-556).
\code{length(peer_transport_pks) + 1L} therefore equals the
cryptographically verified party count (this server + verified
peers); a malicious or misconfigured client cannot forge it.
}
\details{
Pattern: each K-specific *DS function calls
\code{.k2_enforce_K(ss, expected_K = 2L, "fnName")} immediately after
resolving its session state. The check is silent when
\code{peer_transport_pks} is unset (e.g., the function is invoked before
the transport-key handshake -- that path is already guarded by
\code{mpcStoreTransportKeysDS} and the \code{dsvert.require_trusted_peers}
default; this helper is defense-in-depth, not the first line).
}
\keyword{internal}
