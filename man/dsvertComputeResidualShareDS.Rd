% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residualShareDS.R
\name{dsvertComputeResidualShareDS}
\alias{dsvertComputeResidualShareDS}
\title{Compute residual share r = y_ind - p on outcome server}
\usage{
dsvertComputeResidualShareDS(
  p_key,
  indicator_col = NULL,
  data_name = NULL,
  output_key,
  is_outcome_server = FALSE,
  n,
  session_id = NULL
)
}
\arguments{
\item{p_key}{Character. Session slot holding Ring127 share of p per
patient (from Beaver vecmul \code{exp(eta_k) * (1/D)}).}

\item{indicator_col}{Character. Plaintext 0/1 column name on the
outcome server (e.g. \code{"low_ind"}). Ignored on peer.}

\item{data_name}{Character. Data frame name (for indicator column
resolution).}

\item{output_key}{Character. Session slot to store the residual share.}

\item{is_outcome_server}{Logical. When TRUE, subtract y_ind - p_share.
When FALSE, just negate the p share.}

\item{n}{Integer. Length of p / indicator vector.}

\item{session_id}{MPC session id.}
}
\value{
\code{list(stored = TRUE, output_key, n)}.
}
\description{
Local share operation: on the outcome server, subtract a
plaintext 0/1 class-indicator column from a softmax/sigmoid probability
share stored under \code{p_key}. On the peer, just negate the p share.
Output is a new Ring127 residual share under \code{output_key}.

Used by \code{ds.vertMultinomJointNewton} and
\code{ds.vertOrdinalJointNewton} to build residuals for the joint
Beaver matvec gradient per class without any cross-server MPC.
}
