% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coxDiscreteShareDS.R
\name{dsvertCoxEventTimeShareMaskDS}
\alias{dsvertCoxEventTimeShareMaskDS}
\title{Cox event-time risk-set non-disclosive share-mask primitive}
\usage{
dsvertCoxEventTimeShareMaskDS(
  data_name,
  time_var,
  status_var,
  mask_output_key,
  y_output_key,
  target_pk,
  session_id,
  max_event_times = NULL,
  debug = FALSE
)
}
\arguments{
\item{data_name}{Character. Local data frame name on outcome server.}

\item{time_var, status_var}{Character survival time and event columns.}

\item{mask_output_key, y_output_key}{Session slots for this server's shares.}

\item{target_pk}{DCF peer transport public key (base64url).}

\item{session_id}{Active MPC session.}

\item{max_event_times}{Optional public runtime guard.}

\item{debug}{Logical. If TRUE, include local aggregate diagnostics.}
}
\value{
List with sealed mask/y blobs and scalar dimensions.
}
\description{
Share the continuous-time risk-set mask needed for an
event-time Poisson bridge to Breslow Cox without transferring the
patient-level event-time permutation, event vector, or risk-set
membership to a peer. The outcome server computes the unique observed
event-time indices locally and builds a uniform \code{n * J_event}
frame:

\itemize{
\item \code{m_ij = I(time_i >= event_time_j)}
\item \code{y_ij = I(status_i = 1 and time_i = event_time_j)}
}

Both matrices are flattened row-major and split into Ring127 additive
shares between the outcome server and the DCF peer. The returned metadata
contains only guarded scalar dimensions; actual event times, event
indicators, patient order, and risk-set membership are kept local or
share-domain. Sparse tail event times whose at-risk count falls below the
disclosure floor are grouped into the last bin that meets it, so no released
time bin has a single-subject risk set and no event is dropped.
}
