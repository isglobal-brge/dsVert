% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coxPathBDS.R
\name{dsvertCoxPathBCumsumDS}
\alias{dsvertCoxPathBCumsumDS}
\title{Generic strata-aware cumulative sum on an FP share vector}
\usage{
dsvertCoxPathBCumsumDS(
  input_key,
  output_key,
  reverse = TRUE,
  session_id = NULL,
  ring = NULL
)
}
\arguments{
\item{input_key}{Session slot holding the input FP share vector.}

\item{output_key}{Session slot to receive the cumsum FP share.}

\item{reverse}{Logical. TRUE = right-to-left cumulative sum (used for
risk-set weighted averages in Cox); FALSE = left-to-right.}

\item{session_id}{MPC session id.}

\item{ring}{Integer 63 (default) or 127. Falls back to the session-
stored \code{ss$k2_ring} if not supplied.}
}
\value{
list(stored = TRUE, output_key).
}
\description{
Applies k2-fp-cumsum to the share at \code{input_key} (optionally with
strata from ss$k2_cox_strata), storing the result at \code{output_key}.
Local on shares: cumsum(share_A) + cumsum(share_B) = cumsum(share_A + share_B)
by linearity of cumulative sum.
}
