% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsvertClusterDS.R
\name{dsvertExpandClusterWeightsDS}
\alias{dsvertExpandClusterWeightsDS}
\title{Expand a per-cluster weights vector into a per-patient column}
\usage{
dsvertExpandClusterWeightsDS(
  data_name,
  cluster_col,
  weights_per_cluster,
  output_column = "__dsvert_lmm_w"
)
}
\arguments{
\item{data_name}{Character. Aligned data-frame name.}

\item{cluster_col}{Character. Cluster column.}

\item{weights_per_cluster}{Numeric vector (length = n_clusters) in
the order of \code{sort(unique(data[[cluster_col]]))} (matching the
\code{table()} order returned by \code{dsvertClusterSizesDS}).}

\item{output_column}{Column name for the expanded weights vector.}
}
\value{
list(n_expanded, output_column).
}
\description{
Given a vector of weights indexed by cluster in the
order returned by \code{dsvertClusterSizesDS}, write a per-patient
weights column into the data frame. Used by \code{ds.vertLMM} to
implement the REML variance-ratio-weighted inner fit without ever
materialising an \eqn{n}-vector on the client.
}
