% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsvertLMMClusterBroadcastDS.R
\name{dsvertLMMBroadcastClusterIDsDS}
\alias{dsvertLMMBroadcastClusterIDsDS}
\title{Broadcast per-patient cluster IDs to the peer (LMM exact)}
\usage{
dsvertLMMBroadcastClusterIDsDS(
  data_name,
  cluster_col,
  peer_pk,
  session_id = NULL
)
}
\arguments{
\item{data_name}{Aligned data frame.}

\item{cluster_col}{Cluster column.}

\item{peer_pk}{Transport pk of the peer (base64url).}

\item{session_id}{MPC session id.}
}
\value{
list(peer_blob, n_clusters)
}
\description{
On the outcome server (which holds the cluster
membership column plaintext), encode the cluster IDs as an integer
vector and transport-encrypt them to the peer so both DCF parties
can compute per-cluster aggregates of their own r / r^2 shares.
Original cluster labels are not returned to the analyst client or
sent to the peer.

This is the documented LMM inter-server leakage tier: guarded
cluster-ID membership may be shared between the two DCF parties so
they can compute per-cluster aggregates. The shared cluster IDs never
reach the analyst client, and clusters below the configured privacy
threshold fail closed.
}
