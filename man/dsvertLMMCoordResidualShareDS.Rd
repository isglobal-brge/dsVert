% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsvertLMMExactDS.R
\name{dsvertLMMCoordResidualShareDS}
\alias{dsvertLMMCoordResidualShareDS}
\title{LMM cross-server exact residual pipeline — coordinator side}
\usage{
dsvertLMMCoordResidualShareDS(
  data_name,
  y_var,
  x_names,
  betahat_local,
  intercept = 0,
  session_id = NULL,
  frac_bits = 20L
)
}
\arguments{
\item{data_name}{Aligned data-frame name.}

\item{y_var}{Outcome column.}

\item{x_names}{Predictor names on this (outcome) server.}

\item{betahat_local}{Coefficients for the local predictors.}

\item{intercept}{Scalar intercept (default 0).}

\item{session_id}{MPC session id.}

\item{frac_bits}{Ring63 fractional bits (default 20).}
}
\value{
list(stored = TRUE, n).
}
\description{
On the outcome server, consume the peer's relayed share
blob, decrypt it, compute this party's share of the residual
\eqn{r_{ij} = y_{ij} - \alpha - X^{local}_{ij}{}^T\hat\beta^{local}
  - f^{peer,0}_{ij}}, and store it in \code{k2_lmm_exact_r_share}.
The peer side has \eqn{r^{peer} = -f^{peer,1}_{ij}} in its
\code{k2_lmm_exact_peer_share} slot. Sum:
\eqn{r^0 + r^1 = y - \alpha - X\hat\beta} which is the true
residual. Subsequent Beaver vecmul on
\code{k2_lmm_exact_r_share} with itself yields \eqn{r^2} shares
which the caller sums per cluster on the outcome server via
\code{\link{dsvertLMMExactClusterR2DS}}.
}
