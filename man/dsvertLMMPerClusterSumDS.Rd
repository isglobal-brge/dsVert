% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsvertLMMClusterBroadcastDS.R
\name{dsvertLMMPerClusterSumDS}
\alias{dsvertLMMPerClusterSumDS}
\title{Per-cluster FP sum of a session share vector (LMM exact)}
\usage{
dsvertLMMPerClusterSumDS(share_key, session_id = NULL, frac_bits = 20L)
}
\arguments{
\item{share_key}{Session slot holding the n-vector FP share.}

\item{session_id}{MPC session id.}

\item{frac_bits}{Ring63 fractional bits (default 20).}
}
\value{
list(per_cluster_fp: K-vector of base64 FP scalars,
cluster_sizes, n_clusters).
}
\description{
Sum \code{ss[[share_key]]} within each cluster defined
by \code{ss$k2_lmm_cluster_ids}, returning one base64 FP scalar
share per cluster. Linear op preserves additive sharing: the two
parties' outputs aggregate (client-side k2-ring63-aggregate) to the
per-cluster plaintext sum.

Aggregates only; no per-patient information returns to the client.
}
