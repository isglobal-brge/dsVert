% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nbFullRegScoreDS.R
\name{dsvertNBEtaSealDS}
\alias{dsvertNBEtaSealDS}
\title{Per-patient mu seal on non-label server for NB full-reg theta MLE}
\usage{
dsvertNBEtaSealDS(
  data_name,
  x_vars,
  beta_values,
  target_pk,
  session_id = NULL,
  allow_disclosive_legacy = FALSE
)
}
\arguments{
\item{data_name}{Character. Data frame with the feature columns.}

\item{x_vars}{Character vector. Non-label feature names on this server.}

\item{beta_values}{Numeric vector same length as \code{x_vars}: the
non-label beta-slice from a prior Poisson fit (client-provided, since
beta is revealed at convergence).}

\item{target_pk}{Character. Transport PK (base64url) of the label
server that should receive the sealed \code{eta_nl} vector.}

\item{session_id}{Character.}

\item{allow_disclosive_legacy}{Logical. Must be \code{TRUE} to run this
archived helper, and the server must also set
\code{options(dsvert.allow_disclosive_nb_full_reg = TRUE)} or
\code{DSVERT_ALLOW_DISCLOSIVE_NB_FULL_REG=true}. The non-disclosive
replacement is \code{ds.vertNBFullRegTheta(variant = "full_reg_nd")}.}
}
\value{
List with \code{sealed} (base64url blob).
}
\description{
Computes \eqn{\eta_i^s = X_i^s \beta} on this (non-
label) server from the locally-held feature partition and a client-
supplied beta-slice, then transport-seals the vector for the label
server. Used by \code{ds.vertNBFullRegTheta(variant="full_reg")}
to get per-patient mu_i to the outcome server without revealing
them to the client.

Inter-server leakage (documented in P3 budget): the label server
learns \code{eta_nl_i} per patient after decryption. Equivalent
to sharing the non-label's BLUE fitted value with the outcome
holder -- a weaker disclosure than raw features. The client still
sees only scalar aggregates (Sumpsi, Sumlog terms).
}
