% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ordinalJointScoreDS.R
\name{dsvertOrdinalExtractXColumnDS}
\alias{dsvertOrdinalExtractXColumnDS}
\title{Extract column j of an nxp Ring127 share matrix into n-vector slot}
\usage{
dsvertOrdinalExtractXColumnDS(
  matrix_key,
  n,
  p,
  col_idx,
  output_key,
  session_id
)
}
\arguments{
\item{matrix_key}{character. Source flat n*p Ring127 share slot.}

\item{n}{integer. Number of rows.}

\item{p}{integer. Number of columns.}

\item{col_idx}{integer. 1-indexed column to extract.}

\item{output_key}{character. Destination length-n share slot.}

\item{session_id}{MPC session id.}
}
\value{
list(stored = TRUE, n, output_key).
}
\description{
The K=2 X share is stored as a single n*p flat row-major
Ring127 share (16 bytes per entry). For \code{.ring127_vecmul} operations
on per-column X slices, we need length-n session slots. This primitive
gathers row-major indices \verb{[col_idx, p+col_idx, 2p+col_idx, ...]}
from the flat share into a new slot. This is pure local share
rearrangement; the additive-share property is preserved row-by-row.
}
