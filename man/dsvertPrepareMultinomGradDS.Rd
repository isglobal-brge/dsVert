% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multinomJointDS.R
\name{dsvertPrepareMultinomGradDS}
\alias{dsvertPrepareMultinomGradDS}
\title{Prepare softmax-gradient inputs by copying shares into the
canonical \code{secure_mu_share} / \code{k2_y_share_fp} slots}
\usage{
dsvertPrepareMultinomGradDS(
  residual_key,
  is_outcome_server,
  n,
  session_id = NULL
)
}
\arguments{
\item{residual_key}{Session slot holding a Ring127 residual share.}

\item{is_outcome_server}{Whether this server holds the outcome.}

\item{n}{Length of residual vector.}

\item{session_id}{MPC session id.}
}
\value{
\code{list(stored = TRUE)}.
}
\description{
Used by \code{ds.vertMultinomJointNewton} to reuse the
existing \code{glmRing63GenGradTriplesDS} + \code{k2GradientR1DS}
\itemize{
\item \code{k2GradientR2DS} matvec pipeline on a per-class residual
share already computed via \code{dsvertComputeResidualShareDS}.
}
In K>=3 deployments, this still runs only on the two selected DCF
parties; the other servers provide encrypted additive shares and/or
Beaver triples.

The gradient pipeline computes \eqn{X^\top (\mu - y)} internally,
so this helper:
\itemize{
\item Sets \code{secure_mu_share = 0} (zero share).
\item Sets \code{k2_y_share_fp = -residual_share} so the
pipeline's \eqn{\mu - y = 0 - (-r) = r} gives the right
direction without further sign flipping.
}
}
