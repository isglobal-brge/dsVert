% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glmHELinkDS.R
\name{glmHEBlockUpdateDS}
\alias{glmHEBlockUpdateDS}
\title{GD block update with decrypted gradient (HE-Link mode)}
\usage{
glmHEBlockUpdateDS(beta_current, gradient, alpha = 0.1, lambda = 1e-04, n_obs)
}
\arguments{
\item{beta_current}{Numeric vector. Current coefficients (length p_k).}

\item{gradient}{Numeric vector. Decrypted gradient g_k (length p_k).}

\item{alpha}{Numeric. Learning rate (step size). Default 0.1.}

\item{lambda}{Numeric. L2 regularization parameter. Default 1e-4.}

\item{n_obs}{Integer. Number of observations (for gradient normalization).}
}
\value{
List with:
\itemize{
\item \code{beta}: Updated coefficient vector (length p_k)
}
}
\description{
After threshold decryption reveals the p_k-length gradient, this function
performs a simple gradient descent update:
}
\details{
\deqn{\beta_{new} = \beta_{old} + \alpha \cdot (g_k / n - \lambda \cdot \beta_{old})}

Unlike \code{\link{glmBlockSolveDS}} which uses Newton/IRLS updates
(requiring X^T W X and plaintext weights w), GD only needs the gradient
scalar per feature. This avoids transmitting the IRLS weight vector w,
which would require either plaintext mu (privacy leak) or an additional
encrypted computation.
}
