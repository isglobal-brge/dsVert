% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glmSecureRouting.R
\name{glmSecureGradientDS}
\alias{glmSecureGradientDS}
\title{Compute encrypted GLM gradient with transport-encrypted mu/w/v}
\usage{
glmSecureGradientDS(
  data_name,
  x_vars,
  encrypted_mwv = NULL,
  num_obs,
  session_id = NULL
)
}
\arguments{
\item{data_name}{Character. Name of the data frame with local features.}

\item{x_vars}{Character vector. Feature column names on this server.}

\item{encrypted_mwv}{Character or NULL. Transport-encrypted (mu, w, v)
blob (base64url). If NULL, reads from blob storage under key \code{"mwv"}
(set via \code{\link{mheStoreBlobDS}}).}

\item{num_obs}{Integer. Number of observations.}

\item{session_id}{Character or NULL. UUID for session-scoped storage
isolation. Default NULL uses legacy shared storage.}
}
\value{
List with:
\itemize{
\item \code{encrypted_gradients}: Character vector of CKKS-encrypted
gradient components (base64url), one per feature.
\item \code{ct_hashes}: SHA-256 hashes registered in the Protocol
Firewall for subsequent threshold decryption.
}
}
\description{
Non-label server function for the secure routing protocol. Decrypts the
(mu, w, v) blob from the coordinator using this server's X25519 transport
SK, then computes the CKKS-encrypted gradient \eqn{g_k = X_k^T (v \cdot
(ct_y - mu))} using stored \code{ct_y} and local features.
}
\seealso{
\code{\link{glmCoordinatorStepDS}} which produces the (mu, w, v) blob,
\code{\link{glmSecureBlockSolveDS}} which uses the decrypted gradient
}
