% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glmStandardizeDS.R
\name{glmStandardizeDS}
\alias{glmStandardizeDS}
\title{Standardize Features for GLM (Server-Side)}
\usage{
glmStandardizeDS(
  data_name,
  output_name,
  x_vars,
  y_var = NULL,
  session_id = NULL,
  skip_standardize = FALSE,
  mode = "full",
  missing = "fail",
  numeric_family = "gaussian",
  numeric_ring = 63L,
  numeric_y_var = y_var
)
}
\arguments{
\item{data_name}{Character. Name of the source data frame.}

\item{output_name}{Character. Name for the standardized data frame.}

\item{x_vars}{Character vector. Feature columns to standardize.}

\item{y_var}{Character or NULL. Response variable to standardize (Gaussian only).}

\item{session_id}{Character or NULL. UUID for session-scoped storage
isolation. Default NULL uses global shared storage.}

\item{skip_standardize}{Logical. If TRUE, skip server-side standardisation (used by callers that pre-standardised).}

\item{mode}{Character. One of \code{"full"}, \code{"scale_only"}, or
\code{"none"}.}

\item{missing}{Character. Missing-data contract. \code{"fail"} (default)
refuses incomplete analysis columns so missing values cannot be silently
converted to zero in fixed-point arithmetic. \code{"mean_impute"} is an
explicit compatibility policy that imputes the local column mean before
applying the requested scaling.}

\item{numeric_family, numeric_ring}{Custodian numeric-policy context used to
validate the values that will be fixed-point encoded.}

\item{numeric_y_var}{Response column to validate even when it is not being
standardized (as for binomial and Poisson models).}
}
\value{
List with x_means, x_sds, y_mean (if y_var), y_sd (if y_var)
}
\description{
Standardizes specified columns of a data frame (center + scale)
and stores the result as a new data frame. Returns column means and
standard deviations for the client to unstandardize coefficients after
convergence. For Gaussian family, also standardizes the response variable.
}
