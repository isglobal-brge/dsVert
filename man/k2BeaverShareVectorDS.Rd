% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beaverShareUtilsDS.R
\name{k2BeaverShareVectorDS}
\alias{k2BeaverShareVectorDS}
\title{Share a session FP vector with the peer (additive 2-party split)}
\usage{
k2BeaverShareVectorDS(
  variable,
  n,
  levels,
  peer_pk,
  session_id = NULL,
  frac_bits = 20L,
  ring = 63L
)
}
\arguments{
\item{variable}{Character. One-hot variable whose FP vector is shared.}

\item{n}{Number of rows in the one-hot matrix.}

\item{levels}{Number of levels in the one-hot matrix.}

\item{peer_pk}{Transport pk of the peer (base64url).}

\item{session_id}{MPC session id.}

\item{frac_bits}{Ring63 fractional bits (default 20).}

\item{ring}{Integer ring selector, 63 or 127.}
}
\value{
list(peer_blob) -- sealed payload for relay.
}
\description{
Reads the one-hot FP vector derived from \code{variable} in the
MPC session, splits it into two additive shares, retains this party's share,
and returns a transport-encrypted typed transfer for the peer.

Used by \code{ds.vertChisqCross} to share each one-hot indicator
matrix between the DCF parties before the per-cell Beaver product.
}
\keyword{internal}
