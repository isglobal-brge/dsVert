% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beaverVecmulDS.R
\name{k2BeaverVecmulGenTriplesDS}
\alias{k2BeaverVecmulGenTriplesDS}
\title{Generate n-length Beaver triples for element-wise Ring63 product}
\usage{
k2BeaverVecmulGenTriplesDS(
  dcf0_pk,
  dcf1_pk,
  n,
  session_id = NULL,
  frac_bits = 20L,
  ring = 63L,
  dealer_party = NULL
)
}
\arguments{
\item{dcf0_pk, dcf1_pk}{Base64url transport public keys of the two DCF
parties.}

\item{n}{Vector length.}

\item{session_id}{MPC session id (used only to drive the RNG seed).}

\item{frac_bits}{Ring63 fractional bits (default 20). At ring=127 the
handler defaults to fracBits=50 regardless of this argument.}

\item{ring}{Integer 63 (default) or 127. Routes through the Uint128
Ring127 handler when 127 (task #116 Cox/LMM STRICT migration).}

\item{dealer_party}{Optional integer 0/1. When the dealer is also one of
the two DCF parties, store that party's triple share directly in the
current session and only return the encrypted blob for the peer.}
}
\value{
list(triple_blob_0, triple_blob_1) -- both base64url sealed
payloads for relay to party 0 and party 1.
}
\description{
Dealer-only aggregate. Samples n element-wise Beaver
triples \eqn{(a_i, b_i, c_i = a_i b_i)} in Ring63, splits each into
two additive shares, and emits two self-contained payloads sealed
(transport-encrypted) to the two DCF parties' transport public keys.
The client relays each blob to the corresponding party via
\code{mpcStoreBlobDS} under the key \code{"k2_beaver_vecmul_triple"};
each party then consumes it via
\code{\link{k2BeaverVecmulConsumeTripleDS}}.

DEPRECATED / DEALER PATH (F2/F17): this generates the full (a,b,c) triple
in one server's process. That server (the dealer) knows a and b, and from
the online openings d=x-a, e=y-b it can reconstruct the peer's operands.
The earlier claim of "inter-party leakage: none" was FALSE whenever the
dealer is also a computing party (the default). Dealer mode is removed from
the client orchestration; the sole Beaver backend is now the dealer-free
IKNP OT-extension. This handler is retained only for legacy/testing and is
not reached by any product analysis route.
}
