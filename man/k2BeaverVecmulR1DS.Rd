% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beaverVecmulDS.R
\name{k2BeaverVecmulR1DS}
\alias{k2BeaverVecmulR1DS}
\title{Beaver vecmul round 1}
\usage{
k2BeaverVecmulR1DS(
  peer_pk,
  x_key,
  y_key,
  n,
  session_id = NULL,
  frac_bits = 20L
)
}
\arguments{
\item{peer_pk}{Base64url transport pk of the peer.}

\item{x_key, y_key}{Session keys containing this party's FP shares.}

\item{n}{Vector length.}

\item{session_id}{MPC session id.}

\item{frac_bits}{Ring63 fractional bits (default 20).}
}
\value{
list(peer_blob) — base64url sealed payload for peer relay.
}
\description{
Per-party aggregate. Reads the own x- and y-share FP
vectors from the session (under \code{x_key} and \code{y_key}) and
the own triple share, computes the masked
\eqn{d^{own} = x^{own} - a^{own}},
\eqn{e^{own} = y^{own} - b^{own}},
and transport-encrypts \code{(d^{own}, e^{own})} to the peer's
public key. The client relays the returned \code{peer_blob} to the
other party via \code{mpcStoreBlobDS} under
\code{"k2_beaver_vecmul_peer_masked"}, ready for round 2.
}
