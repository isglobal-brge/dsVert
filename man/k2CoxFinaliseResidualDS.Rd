% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beaverVecmulDS.R
\name{k2CoxFinaliseResidualDS}
\alias{k2CoxFinaliseResidualDS}
\title{Cox residual finalisation: r = delta - mu\emph{G (party 0) / -mu}G (party 1)}
\usage{
k2CoxFinaliseResidualDS(is_party0, session_id = NULL, frac_bits = 20L)
}
\arguments{
\item{is_party0}{Logical.}

\item{session_id}{MPC session id.}

\item{frac_bits}{Ring63 fractional bits.}
}
\value{
list(done = TRUE).
}
\description{
Assuming the Beaver vecmul has populated
\code{ss$k2_cox_mu_g_share_fp} with this party's share of
\eqn{\mu \odot G}, compute the Cox residual share
\eqn{r^0 = \delta - (\mu G)^0,  r^1 = -(\mu G)^1}
(recall \eqn{\delta} is plaintext at BOTH parties via
\code{k2SetCoxTimesDS} / \code{k2ReceiveCoxMetaDS}) and store it in
\code{secure_mu_share} so the existing \code{X^T r} Beaver gradient
machinery can consume it without modification.
}
