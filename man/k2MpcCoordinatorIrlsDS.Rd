% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/k2MpcDS.R
\name{k2MpcCoordinatorIrlsDS}
\alias{k2MpcCoordinatorIrlsDS}
\title{Coordinator (label server) Gauss-Seidel IRLS step for K=2}
\usage{
k2MpcCoordinatorIrlsDS(
  data_name,
  y_var,
  x_vars,
  beta_current,
  non_label_pk,
  family = "binomial",
  lambda = 1e-04,
  n_obs = NULL,
  intercept = FALSE,
  session_id = NULL
)
}
\arguments{
\item{data_name}{Character. Name of the (standardized) data frame.}

\item{y_var}{Character. Response variable name.}

\item{x_vars}{Character vector. Feature column names on this server.}

\item{beta_current}{Numeric vector. Current label-server coefficients.}

\item{non_label_pk}{Character. Non-label server's transport PK (base64url).}

\item{family}{Character. "binomial" or "poisson".}

\item{lambda}{Numeric. L2 regularization parameter.}

\item{n_obs}{Integer or NULL. Number of observations (unused, kept for API compat).}

\item{session_id}{Character or NULL.}
}
\value{
List with:
\itemize{
\item \code{beta}: updated coefficient vector
\item \code{encrypted_wr}: base64url transport-encrypted (w, residual) blob
}
}
\description{
Performs the coordinator's IRLS block update using fresh eta from the
non-label server. After updating beta_label, recomputes mu/w/residual
with the NEW beta (the Gauss-Seidel key step) and transport-encrypts
(w, residual) for the non-label server.
}
\details{
This mirrors \code{\link{glmCoordinatorStepDS}} for K>=3 but uses
transport encryption instead of CKKS+threshold decryption.
}
