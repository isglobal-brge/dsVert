% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/k2MpcDS.R
\name{k2MpcLinkEvalDS}
\alias{k2MpcLinkEvalDS}
\title{Evaluate piecewise link function on eta_total (coordinator)}
\usage{
k2MpcLinkEvalDS(
  family,
  peer_share_key = "mpc_peer_eta_share",
  peer_pk,
  frac_bits = 20L,
  session_id = NULL
)
}
\arguments{
\item{family}{Character. "binomial" or "poisson".}

\item{peer_share_key}{Character. Blob key where the peer's eta share
(transport-encrypted) is stored.}

\item{peer_pk}{Character. Peer's transport public key (base64url) for
encrypting the output mu share.}

\item{frac_bits}{Integer. Fixed-point fractional bits.}

\item{session_id}{Character or NULL.}
}
\value{
List with:
\itemize{
\item \code{peer_mu_share_enc}: base64url transport-encrypted peer's mu share
}
}
\description{
Called on the coordinator (label server) after receiving the non-label
server's transport-encrypted eta share. Reconstructs eta_total from both
shares, evaluates the piecewise sigmoid/exp, and splits the result into
new shares.
}
