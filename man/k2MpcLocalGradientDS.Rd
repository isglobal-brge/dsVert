% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/k2MpcDS.R
\name{k2MpcLocalGradientDS}
\alias{k2MpcLocalGradientDS}
\title{Reconstruct full residual and compute own gradient}
\usage{
k2MpcLocalGradientDS(
  data_name,
  x_vars,
  num_obs,
  frac_bits = 20L,
  session_id = NULL
)
}
\arguments{
\item{data_name}{Character. Name of the data frame.}

\item{x_vars}{Character vector. Feature column names.}

\item{num_obs}{Integer. Number of observations.}

\item{frac_bits}{Integer. Fixed-point fractional bits.}

\item{session_id}{Character or NULL.}
}
\value{
List with:
\itemize{
\item \code{gradient}: numeric vector (length p_k)
\item \code{weights}: numeric vector (length n), IRLS weights mu*(1-mu)
}
}
\description{
Receives the peer's residual share (from blob storage), reconstructs the
full residual vector, and computes the local gradient g_k = X_k^T * r / n.
The residual is discarded after gradient computation.
}
\details{
Security: the residual (mu - y) does not reveal eta_nonlabel because
mu = link^{-1}(eta_total) is a non-invertible transformation.
}
