% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ring127SplinelessDS.R
\name{k2Ring127AffineCombineDS}
\alias{k2Ring127AffineCombineDS}
\title{Ring127 affine combine -- server-side local op for Horner/NR
orchestration.}
\usage{
k2Ring127AffineCombineDS(
  a_key = NULL,
  b_key = NULL,
  sign_a = 0L,
  sign_b = 0L,
  public_const_fp = NULL,
  is_party0 = FALSE,
  output_key,
  n,
  session_id = NULL
)
}
\arguments{
\item{a_key}{Session slot holding the first Ring127 share vector (base64
Uint128 at 16 B/elt). Ignored when \code{sign_a == 0}; may be
\code{NULL} in that case.}

\item{b_key}{Session slot holding the second Ring127 share vector.
Ignored when \code{sign_b == 0}.}

\item{sign_a}{Integer in \{-1, 0, +1\}. Sign coefficient for the a slot.}

\item{sign_b}{Integer in \{-1, 0, +1\}. Sign coefficient for the b slot.}

\item{public_const_fp}{Base64 string encoding a single Ring127 FP Uint128
(16 B). Added to every element on party 0 only. \code{NULL} for no
constant.}

\item{is_party0}{Logical. TRUE for the coordinator (outcome-holder)
party; controls whether \code{public_const_fp} is applied.}

\item{output_key}{Session slot name to store the resulting share vector.}

\item{n}{Integer vector length.}

\item{session_id}{MPC session identifier.}
}
\value{
list(stored = TRUE, output_key, n).
}
\description{
Computes, on one party's Ring127 shares:
\deqn{out[i] = sign_a \cdot a[i] + sign_b \cdot b[i] +
                 (public_const \text{ if party 0 else } 0)}
where \eqn{sign_a, sign_b \in \{-1, 0, +1\}}. The result is stored back
into the session slot named \code{output_key}. No cross-party
communication -- the client orchestrates one such call on each party
per Horner / NR iteration.

Called by \code{dsVertClient:::.exp127_round} and
\code{dsVertClient:::.recip127_round} (task #116 step 5c(I-c)). Not
Ring63-applicable; fails fast if invoked in a session whose ring is
not 127.
}
