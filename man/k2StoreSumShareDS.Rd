% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beaverShareUtilsDS.R
\name{k2StoreSumShareDS}
\alias{k2StoreSumShareDS}
\title{Store a scalar sum share without returning it to the client}
\usage{
k2StoreSumShareDS(
  source_key,
  output_key,
  append = FALSE,
  session_id = NULL,
  ring = NULL
)
}
\arguments{
\item{source_key}{Session key containing the FP share vector to sum.}

\item{output_key}{Session key where the scalar share or concatenated scalar
share vector is stored.}

\item{append}{Logical. Append this scalar to an existing FP vector.}

\item{session_id}{Active MPC session identifier.}

\item{ring}{Optional ring selector ("ring63"/"ring127"); defaults from the
session when available.}
}
\value{
list(stored, output_key, length).
}
\description{
Local linear reduction of a session-stored FP share vector.
Unlike \code{k2BeaverSumShareDS}, this helper keeps the scalar share in
the server session. With \code{append = TRUE}, successive scalar sums are
concatenated into one FP share vector. This supports pre-release MPC
threshold checks where the analyst must not receive both parties' scalar
shares before the disclosure guard passes.
}
