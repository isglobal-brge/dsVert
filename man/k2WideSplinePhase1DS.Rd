% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/k2WideSplineFullDS.R
\name{k2WideSplinePhase1DS}
\alias{k2WideSplinePhase1DS}
\title{Wide spline Phase 1: DCF masked values}
\usage{
k2WideSplinePhase1DS(
  party_id = 0L,
  family = "binomial",
  num_intervals = NULL,
  frac_bits = 20L,
  ring = 63L,
  session_id = NULL
)
}
\arguments{
\item{party_id}{Integer. 0 (fusion) or 1 (coordinator).}

\item{family}{Character. "binomial" or "poisson".}

\item{num_intervals}{Integer. Spline intervals (50 sigmoid, 100 exp).}

\item{frac_bits}{Integer. Fractional bits (default 20 for Ring63;
up to 126 for Ring127, though typically 50).}

\item{ring}{Integer 63 (default) or 127. Selects the MPC secret-share
ring. Ring127 is selected by the Cox/LMM STRICT closure path (task
#116). When ring == 127, per-element records are 16 bytes (Uint128)
instead of 8 bytes (int64); the downstream Go handler branches on
\code{ring = "ring127"} in the JSON input.}

\item{session_id}{Character or NULL.}
}
\value{
List with dcf_masked (base64url).
}
\description{
Computes DCF comparison challenges from eta shares. The masked values
are exchanged between DCF parties for phase 2.
}
