% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mheFuseDecryptDS.R
\name{mheFuseDecryptDS}
\alias{mheFuseDecryptDS}
\title{MHE Fuse Decryption (Server-Side)}
\usage{
mheFuseDecryptDS(partial_decryptions, rows, cols, log_n = 14, log_scale = 40)
}
\arguments{
\item{partial_decryptions}{Character vector. Base64-encoded partial decryption
shares from all parties.}

\item{rows}{Integer. Number of rows in the result matrix.}

\item{cols}{Integer. Number of columns in the result matrix.}

\item{log_n}{Integer. Ring dimension parameter (must match encryption).}

\item{log_scale}{Integer. Scale parameter (must match encryption).}
}
\value{
A list containing:
\itemize{
\item \code{data}: The decrypted matrix as a 2D array
}
}
\description{
Server-side aggregate function that combines partial decryption
shares to recover the plaintext result.
}
\details{
This function combines partial decryption shares from ALL parties
to recover the plaintext. This is the final step in the MHE protocol
where the encrypted computation result is revealed.
}
\note{
This function is typically called on one server to perform
the final combination. The resulting plaintext is the aggregate
statistic (e.g., correlation matrix), NOT individual data.
}
\section{Security}{

This function can ONLY succeed if partial decryption shares from
ALL parties are provided. Missing even one share makes decryption
impossible.
}

